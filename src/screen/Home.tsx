import * as React from "react";
import { mergeData } from "../utils";

const Home: React.FC = () => {

    console.log(mergeData())

  return (
    <p>Initialize Project</p>
  );
};

export default Home;
